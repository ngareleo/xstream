// Variant picker events. Currently a passive component — no events emitted
// beyond the onSelect callback to its caller. Placeholder for future Nova
// instrumentation if a "switched copy" telemetry signal becomes useful.
export const filmVariantsEvents = {} as const;
