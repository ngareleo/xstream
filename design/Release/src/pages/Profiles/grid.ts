/**
 * Shared grid template for the Profiles explorer. The column header,
 * each ProfileRow, and each FilmRow all share this layout — keep them
 * in lockstep by importing this constant rather than duplicating the
 * literal string.
 */
export const PROFILE_GRID_COLUMNS = "30px 1.3fr 0.7fr 0.6fr 80px";
