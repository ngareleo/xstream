# Client Observability

| File | Hook |
|---|---|
| [`00-Spans.md`](00-Spans.md) | Client span + log reference table: `playback.session`, `chunk.stream`, `transcode.request`, `buffer.backpressure`, `playback.stalled`, `graphql.request`, and the `playback.*` log events. |
