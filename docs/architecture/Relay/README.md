# Relay

GraphQL + Relay contract: how queries, fragments, and mutations are shaped on both sides of the wire.

| File | Hook |
|---|---|
| [`00-Fragment-Contract.md`](00-Fragment-Contract.md) | `<ComponentName>_<propName>` fragment naming; operation names mirror filenames; `useLazyLoadQuery` in pages only; presenters own shape conversion. |
