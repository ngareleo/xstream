# Bundle Chunks

| File | Hook |
|---|---|
| [`00-Strategy.md`](00-Strategy.md) | Rsbuild `chunkSplit` groups, upgrade-cadence principle, regex anchor invariant, `enforce` for sub-minSize chunks, `bun run analyze`. |
