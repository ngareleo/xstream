# Invariants

| File | Hook |
|---|---|
| [`00-Never-Violate.md`](00-Never-Violate.md) | The 10 rules: SQL through `db/queries/` only, init segment first, `appendBuffer` guard, `path` unique, `endOfStream` required, `content_fingerprint NOT NULL`, Relay ID encoding, one resolver per field, etc. |
