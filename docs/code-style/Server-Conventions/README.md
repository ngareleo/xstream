# Server Conventions

| File | Hook |
|---|---|
| [`00-Patterns.md`](00-Patterns.md) | Explicit resolver return types; presenters own shape conversion; resolvers → services → `db/queries/`; one resolver per field; `setFfmpegPath` is module-global. |
