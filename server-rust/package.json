{
  "name": "server-rust",
  "private": true,
  "scripts": {
    "dev": "bun ../scripts/dev-server.ts",
    "build": "bun ../scripts/cargo.ts build --release",
    "lint": "bun ../scripts/cargo.ts build --quiet"
  }
}
